import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'App_02_mBridge',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
