import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'App_04_BiblioReference',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
