import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'App_03_QuizApp',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
